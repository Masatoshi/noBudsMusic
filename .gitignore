# Generated from project.yml by XcodeGen. Run `just generate`.
*.xcodeproj/
*.xcworkspace/

# Icon build intermediate; Resources/AppIcon.icns is committed
build/AppIcon.iconset/

# Xcode
DerivedData/
build/
*.xcuserstate

# macOS
.DS_Store

# Local signing identity and other machine-specific overrides
.env

# Agent-facing working files. Kept locally so future sessions have the context;
# not published, because they are instructions to a tool rather than to a reader.
# The parts worth publishing were moved to docs/macos-notes.md.
AGENTS.md
.agents/

# Local agent / editor state
logs/
.claude/settings.local.json
